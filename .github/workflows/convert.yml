name: Convert VPN Configs

on:
  push:
    branches:
      - main
    paths:
      - 'vpn.txt'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Run conversion script
        env:
          GEO_API_KEY: ${{ secrets.GEO_API_KEY }}
        run: node convert.js

      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add vpn.json vpns.json vpn.yml vpn64.txt vpnf.json
          git diff --staged --quiet || git commit -m "Auto-update all VPN subscription formats"
          git push
